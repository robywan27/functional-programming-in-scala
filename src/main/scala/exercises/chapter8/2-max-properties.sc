// max: List[Int] => Int

/*
  Properties
*/

// 1. List().max - there is no max value in an empty list: throw an exception/return null/...
// 2. List(1).max == 1 - max of list with one element is the element
// 3. List(1, 1, 1, 1, 1).max == 1 - max of list with equal elements is the element
// 4. List(1, 2, 3, 4, 5).max == List(1, 2, 3, 4, 5).last - max of sorted list is the last element; for reverse is first element
// 5. List(6, 2, 19, 35, 2).max ==

